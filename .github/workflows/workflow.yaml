name: workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
      - uses: action/checkout@v4
      - uses: action/setup-node@v4
        
      - run: npm install

      - name: setup
        run: make setup

      - name: test
        run: make test

      - name: lint
        run: make lint
